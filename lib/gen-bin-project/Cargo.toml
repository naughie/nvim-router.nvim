[package]
name = "gen-bin-project"
version = "0.1.0"
edition = "2024"

[profile.release]
strip = "symbols"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.9"

log = "0.4"
simplelog = "0.12"
